openapi: 3.0.0
info:
  title: Warehouse Receipt API
  version: 1.0.0
  description: API documentation for Warehouse Receipt module - Receipt management and status change tracking. Audit logs are automatically created when warehouse receipt status changes (INITIATE → IN_PROGRESS → COMPLETED/HOLD). Receipts are created automatically when ID verifications are processed.

tags:
  - name: Receipts
    description: Warehouse receipt CRUD operations

paths:
  /api/warehouse-receipt/temp:
    post:
      tags:
        - Receipts
      summary: Create temporary warehouse receipt
      description: "Create a temporary warehouse receipt that will be used as a base for the actual receipt. Returns all temporary receipt data.\n\nAuto-generated fields:\n- receiptNumber (from sequence)\n- receiptDate (current timestamp)\n- createdBy (from authenticated user)\n- createdAt (current timestamp)"
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTemporaryWarehouseReceipt"
            example:
              verificationId: 100000033
              shipper: "ABC Logistics Inc"
              customerId: 51
              stationId: 93
              carrierId: 52
              status: "INITIATE"
              receivedBy: "John Warehouse"
              location: "Warehouse A, Zone 1"
              destination: "Customer Final Destination"
              proNumber: "PRO1234511"
              packageId: "PKG-001"
      responses:
        "201":
          description: Temporary warehouse receipt created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Temporary warehouse receipt created successfully"
                  data:
                    $ref: "#/components/schemas/WarehouseReceiptTemp"
        "400":
          description: Missing required fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/batch:
    post:
      tags:
        - Receipts
      summary: Batch process warehouse receipts (update and/or create with optional image uploads)
      description: "Process multiple warehouse receipts in a single batch transaction. Supports both JSON and multipart/form-data with images.\n\n**Send as JSON** for batch operations without images.\n\n**Send as multipart/form-data** for batch operations with image uploads:\n- Field: `batchData` (JSON string without images)\n- Files: Image files with field names: `freight-{receiptIndex}-{freightIndex}-{imageIndex}`\n  - Example: `freight-0-0-0` → receipts[0].freightDetails[0].images[0]\n  - Example: `freight-0-1-0` → receipts[0].freightDetails[1].images[0]\n\nArray structure: Each item must have `receipt` (object) and `freightDetails` (array).\n\n**System-managed fields** (auto-handled, do NOT provide in request):\n- createdAt, createdBy: auto-set on creation\n- updatedAt, updatedBy: auto-set on update\n- documentId: auto-generated on creation\n- entityId, noteThreadId: auto-created on creation\n\n**Required fields in payload:**\n- receiptId: required for UPDATE operations\n- receiptNumber: required for all operations (for reference/audit)\n- All updateable receipt fields and freightDetails array\n\n**UPDATE Operation** (receipt has receiptId):\n- Only updateable fields are applied to the receipt\n- All freight items are deleted and replaced with new ones from freightDetails\n- Automatically sets updatedBy to authenticated user\n- Automatically sets updatedAt to current timestamp\n\n**CREATE Operation** (receipt has no receiptId):\n- Creates new receipt with provided receipt data (including receiptNumber)\n- Creates freight items from freightDetails array\n- System fields are auto-generated and managed by backend\n\n**Image Upload**:\n- Optional: send images via multipart/form-data\n- Images stored in `uploads/freight-images/`\n- Supported formats: JPEG, PNG, GIF, WebP (Max 10MB per file)\n- Images linked to freight via freightId in Warehouse_Receipt_Freight_Images table\n- Response includes image paths and metadata for each freight item\n\nAll operations are atomic - either all succeed or all rollback."
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: split
          required: false
          schema:
            type: boolean
          description: "Set to true or false to indicate whether this batch operation is a split operation."
        - in: query
          name: parentReceiptId
          required: false
          schema:
            type: string
          description: "Parent receipt number to associate with the batch receipts."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchProcessWarehouseReceipts"
            example:
              receipts:
                - receipt:
                    receiptId: 25
                    receiptNumber: 100000031
                    receivedBy: null
                    location: null
                    shipper: "ABC Logistics Inc"
                    customerId: 51
                    stationId: 93
                    verificationId: 100000033
                    carrierId: 52
                    piecesInland: 50
                    weightInland: 5000
                    reWeight: null
                    proNumber: "PRO1234511"
                    invoiceNumber: null
                    poNumber: null
                    customerRefNumber: null
                    freightCondition: "Y"
                    documents: "Y"
                    handlingDescription: null
                    destination: null
                    originalDgd: null
                    unNumber: ["123", "333", "3333"]
                    class: ["hello", "hi", "Welcome", "next", "wrap", "cook"]
                    packageId: null
                    properShippingName: null
                    hazardousDescription: null
                    notes: "Handle with care"
                    status: "INITIATE"
                    receiptType: "Regular"
                    bandedSkid: "Y"
                    shrinkWrappedSkid: "Y"
                    shtIppcSkid: "Y"
                    plasticSkid: "Y"
                    hazMat: "Y"
                    labelCount: 0
                    toEmails: ["user@example.com"]
                    cubicMeter: 100
                    driverName: "John Doe"
                  freightDetails:
                    - pieces: 20
                      type: "Box"
                      weight: 2000
                      length: 10
                      width: 10
                      height: 10
                      cubicMeter: 20
                    - pieces: 30
                      type: "Pallet"
                      weight: 3000
                      length: 48
                      width: 40
                      height: 60
                      cubicMeter: 80
                - receipt:
                    receiptNumber: 100000033
                    receivedBy: null
                    location: null
                    shipper: "Global Freight Ltd"
                    customerId: 51
                    stationId: 93
                    verificationId: 100000033
                    carrierId: 52
                    piecesInland: 40
                    weightInland: 4200
                    reWeight: null
                    proNumber: "PRO1234511"
                    invoiceNumber: null
                    poNumber: null
                    customerRefNumber: null
                    freightCondition: "Y"
                    documents: "Y"
                    handlingDescription: null
                    destination: null
                    originalDgd: null
                    unNumber: ["123", "333", "3333"]
                    class: ["hello", "hi", "Welcome", "next", "wrap", "cook"]
                    packageId: null
                    properShippingName: null
                    hazardousDescription: null
                    notes: "This is a note for the receipt"
                    status: "INITIATE"
                    receiptType: "Regular"
                    bandedSkid: "Y"
                    shrinkWrappedSkid: "Y"
                    shtIppcSkid: "Y"
                    plasticSkid: "Y"
                    hazMat: "Y"
                    labelCount: 0
                    toEmails: ["user@example.com"]
                    cubicMeter: 100
                    driverName: "John Doe"
                  freightDetails:
                    - pieces: 10
                      type: "Crate"
                      weight: 1200
                      length: 20
                      width: 15
                      height: 12
                      cubicMeter: 3.6
                    - pieces: 30
                      type: "Pallet"
                      weight: 3000
                      length: 48
                      width: 40
                      height: 55
                      cubicMeter: 70
          multipart/form-data:
            schema:
              type: object
              required:
                - batchData
              properties:
                batchData:
                  type: string
                  format: binary
                  description: "JSON string containing batch receipts array (without images). Images are sent as separate files."
                  example: '{"receipts":[{"receipt":{"receiptId":10,"receiptNumber":100000015,...},"freightDetails":[{"pieces":20,"type":"Box",...}]}]}'
                freight-0-0-0:
                  type: string
                  format: binary
                  description: "Image file for receipts[0].freightDetails[0].images[0]"
                freight-0-0-1:
                  type: string
                  format: binary
                  description: "Image file for receipts[0].freightDetails[0].images[1]"
                freight-0-1-0:
                  type: string
                  format: binary
                  description: "Image file for receipts[0].freightDetails[1].images[0]"
      responses:
        "201":
          description: Batch process completed successfully (with or without images)
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Batch process completed successfully"
                  filesProcessed:
                    type: integer
                    example: 5
                    description: "Number of image files processed (only present when images uploaded)"
                  data:
                    $ref: "#/components/schemas/BatchProcessResponse"
        "400":
          description: Missing required fields or invalid data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/document-upload:
    post:
      tags:
        - Receipts
      summary: Upload warehouse receipt documents
      description: "Upload one or more supporting documents for a warehouse receipt. Each uploaded file is stored on disk and a record is created in Warehouse_Receipt_Documents for the specified receiptId."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - receiptId
              properties:
                receiptId:
                  type: integer
                  description: Warehouse receipt ID to associate the uploaded documents with.
                  example: 100000001
                documents:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more document files to upload. Supports PDF, DOC, DOCX, TXT, JPG, and PNG files.
      responses:
        "201":
          description: Documents uploaded successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Documents uploaded successfully"
                  data:
                    type: object
                    properties:
                      receiptId:
                        type: integer
                        example: 100000001
                      documentIds:
                        type: array
                        items:
                          type: integer
                        example: [1, 2]
                      count:
                        type: integer
                        example: 2
        "400":
          description: Missing receiptId or no files uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt:
    post:
      tags:
        - Receipts
      summary: Create warehouse receipt with freight info
      description: "Create a warehouse receipt with multiple freight details. Automatically creates associated document record and returns the receipt with all freight items.\n\nAuto-generated fields:\n- receiptNumber (from sequence)\n- documentId (from Warehouse_Receipt_Documents)\n- createdBy (from authenticated user)\n- createdAt (current timestamp)"
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWarehouseReceiptWithFreight"
            example:
              receipt:
                receiptDate: "2026-04-06T10:00:00Z"
                shipper: "ABC Logistics Inc"
                customerId: 51
                stationId: 93
                verificationId: 100000021
                carrierId: 52
                proNumber: "PRO-2026-001"
                piecesInland: 50
                weightInland: 5000
                status: "INITIATE"
                entityId: 1
                noteThreadId: 1
                toEmails: ["user@example.com"]
                cubicMeter: 100
              freightDetails:
                - pieces: 20
                  type: "Box"
                  weight: 2000
                  length: 10
                  width: 10
                  height: 10
                  cubicMeter: 20
                - pieces: 30
                  type: "Pallet"
                  weight: 3000
                  length: 48
                  width: 40
                  height: 60
                  cubicMeter: 80
      responses:
        "201":
          description: Warehouse receipt created successfully with freight info
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse receipt created successfully with freight info"
                  data:
                    $ref: "#/components/schemas/WarehouseReceiptWithFreightResponse"
        "400":
          description: Missing required fields or invalid data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

    get:
      tags:
        - Receipts
      summary: List warehouse receipts with pagination and filters
      description: Retrieve all warehouse receipts with optional filtering by status and carrier
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: page
          schema:
            type: integer
            default: 1
          description: Page number for pagination
        - in: query
          name: pageSize
          schema:
            type: integer
            default: 10
          description: Number of records per page
        - in: query
          name: status
          schema:
            type: string
            enum:
              [
                INITIATED,
                ON_HAND,
                PREPARED,
                SCANNED,
                SHIPPED,
                REJECTED,
                ARCHIVED,
              ]
          description: Filter by receipt status
        - in: query
          name: receiptNumber
          schema:
            type: integer
          description: Filter by receipt number
        - in: query
          name: accounting
          schema:
            type: boolean
          description: Filter by accounting status (true for accountOnHold = 'Y', false for accountOnHold = 'N')
        - in: query
          name: startDate
          schema:
            type: string
            format: date
          description: Filter receipts created after this date (inclusive)
        - in: query
          name: endDate
          schema:
            type: string
            format: date
          description: Filter receipts created before this date (inclusive)
        - in: query
          name: customerId
          schema:
            type: integer
          description: Filter by customer ID
        - in: query
          name: stationId
          schema:
            type: integer
          description: Filter by station ID
        - in: query
          name: carrierId
          schema:
            type: integer
          description: Filter by carrier ID
        - in: query
          name: location
          schema:
            type: string
          description: Filter by location (partial match)
        - in: query
          name: proNumber
          schema:
            type: string
          description: Filter by PRO number (partial match)
        - in: query
          name: verificationId
          schema:
            type: integer
          description: Filter by ID verification ID
        - in: query
          name: destination
          schema:
            type: string
          description: Filter by destination (partial match)
        - in: query
          name: packageId
          schema:
            type: string
          description: Filter by package ID (partial match)
        - in: query
          name: customerRefNumber
          schema:
            type: string
          description: Filter by customer reference number (partial match)
      responses:
        "200":
          description: Receipts retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/WarehouseReceipt"
                  pagination:
                    $ref: "#/components/schemas/PaginationInfo"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/export-spreadsheet:
    get:
      tags:
        - Receipts
      summary: Export warehouse receipts to Excel spreadsheet
      description: |
        Export filtered warehouse receipts to an Excel (XLSX) file with the same filters and pagination options as the list endpoint.

        **Features:**
        - Exports receipts with all filterable fields
        - Supports all the same pagination and filtering parameters as the list endpoint
        - Returns file download with filename: `warehouse_receipts.xlsx`
        - Excel file includes headers in bold and cell borders for readability

        **Exported Columns:**
        - Warehouse Receipt ID (receiptNumber)
        - Customer Name (consignee)
        - Carrier
        - Location
        - Pro Number
        - Destination
        - Created Date (formatted as locale string)
        - Status
        - ID Verification Number
        - Package ID
        - Customer Ref Number

        **Response Type:**
        Returns binary Excel file (application/vnd.openxmlformats-officedocument.spreadsheetml.sheet) instead of JSON.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum:
              [
                INITIATED,
                ON_HAND,
                PREPARED,
                SCANNED,
                SHIPPED,
                REJECTED,
                ARCHIVED,
              ]
          description: Filter by receipt status
        - in: query
          name: receiptNumber
          schema:
            type: integer
          description: Filter by receipt number (exact match)
        - in: query
          name: accounting
          schema:
            type: boolean
          description: Filter by accounting hold status (true = on hold, false = not on hold)
        - in: query
          name: startDate
          schema:
            type: string
            format: date
          description: Filter receipts created on or after this date (YYYY-MM-DD)
        - in: query
          name: endDate
          schema:
            type: string
            format: date
          description: Filter receipts created on or before this date (YYYY-MM-DD)
        - in: query
          name: customerId
          schema:
            type: integer
          description: Filter by customer ID
        - in: query
          name: stationId
          schema:
            type: integer
          description: Filter by station ID
        - in: query
          name: carrierId
          schema:
            type: integer
          description: Filter by carrier ID
        - in: query
          name: location
          schema:
            type: string
          description: Filter by location (partial match, case-insensitive)
        - in: query
          name: proNumber
          schema:
            type: string
          description: Filter by PRO number (partial match, case-insensitive)
        - in: query
          name: verificationId
          schema:
            type: integer
          description: Filter by ID verification ID
        - in: query
          name: destination
          schema:
            type: string
          description: Filter by destination (partial match, case-insensitive)
        - in: query
          name: packageId
          schema:
            type: string
          description: Filter by package ID (partial match, case-insensitive)
        - in: query
          name: customerRefNumber
          schema:
            type: string
          description: Filter by customer reference number (partial match, case-insensitive)
      responses:
        "200":
          description: Excel file with warehouse receipts exported successfully
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
              headers:
                Content-Disposition:
                  schema:
                    type: string
                    example: "attachment; filename=warehouse_receipts.xlsx"
                Content-Type:
                  schema:
                    type: string
                    example: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
        "400":
          description: Invalid query parameters or filter values
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error or no records found
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "No Record Found"

  /api/warehouse-receipt/{id}:
    get:
      tags:
        - Receipts
      summary: Get warehouse receipt with all details
      description: |
        Retrieve receipt with freight info, rates, and audit logs. Supports lookup by:
        - **Receipt Number** (default): Pass numeric receiptNumber
        - **PRO Number**: Pass PRO number string with `?searchBy=proNumber` query parameter

        Returns receipt details including associated freight items with images, rate information, and audit logs.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: Receipt identifier - either numeric receiptNumber (default) or PRO number string (when searchBy=proNumber)
          example: "123"
        - in: query
          name: searchBy
          schema:
            type: string
            enum: [receiptNumber, proNumber]
            default: receiptNumber
          description: |
            Specifies the lookup field:
            - `receiptNumber` (default): id parameter is treated as Receipt Number
            - `proNumber`: id parameter is treated as PRO number string
          example: receiptNumber
      responses:
        "200":
          description: Receipt found successfully with all details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: "#/components/schemas/WarehouseReceiptWithDetails"
        "400":
          description: Invalid request - missing id or invalid searchBy parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Receipt not found for the provided id and searchBy parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

    put:
      tags:
        - Receipts
      summary: Update warehouse receipt
      description: |
        Update receipt details, freight items, and image references.
        Supports both JSON and multipart/form-data payloads for hybrid file + metadata updates.
        When status changes, an audit log is automatically created to track the change.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          schema:
            type: string
          required: true
          description: Unique receipt identifier (bigint)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReceiptRequest"
          multipart/form-data:
            schema:
              type: object
              properties:
                receipt:
                  type: string
                  description: |
                    JSON string containing the receipt update payload.
                    Recommended when sending files in multipart form.
                  example: '{"location":"Warehouse B","status":"ON_HAND","piecesInland":20,"updatedBy":1,"freightDetails":[{"freightId":123,"pieces":10,"type":"Pallet","images":["path/to/image.jpg"]}],"removeFreightIds":[456]}'
                freightDetails:
                  type: string
                  description: JSON string array for freight items. Each item may include freightId, pieces, type, dimensions, weight, cubicMeter, images, and removeImagePaths.
                removeFreightIds:
                  type: string
                  description: JSON string array of freight IDs to delete from the receipt.
                badFreightImages:
                  type: string
                  description: JSON string array of bad freight image path values to add.
                removeBadFreightImagePaths:
                  type: string
                  description: JSON string array of bad freight image path values to remove.
                file:
                  type: string
                  format: binary
                  description: |
                    Image files for freight or bad freight conditions.
                    Supports BOTH traditional file uploads AND Base64 strings sent in request body fields.

                    Traditional file upload field names:
                    - freight-{freightIndex}-{imageIndex} (e.g., freight-0-0, freight-0-1 for freight item images)
                    - bad-freight-image-{index} (e.g., bad-freight-image-0 for bad freight images)

                    Base64 mode (like batch API): Send Base64 strings in body fields with matching names
                    - Any body field with name matching above patterns and value starting with "data:image/" or "base64," will be automatically converted to a file
                    - Example: body field "freight-0-0" = "data:image/jpeg;base64,..." → converted to file and attached
                    - Example: body field "bad-freight-image-0" = "base64,..." → converted to file and attached

                    Hybrid mode: Combine both file uploads and Base64 strings in the same request
              encoding:
                receipt:
                  contentType: application/json
                freightDetails:
                  contentType: application/json
                removeFreightIds:
                  contentType: application/json
                badFreightImages:
                  contentType: application/json
                removeBadFreightImagePaths:
                  contentType: application/json
      responses:
        "200":
          description: Receipt updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Receipt updated successfully"
                  data:
                    $ref: "#/components/schemas/WarehouseReceiptWithDetails"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/{receiptId}/location:
    patch:
      tags:
        - Receipts
      summary: Update warehouse receipt location
      description: Update only the location field of a warehouse receipt. This is a targeted update for location changes without affecting other receipt properties.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: receiptId
          schema:
            type: string
          required: true
          description: Unique receipt identifier (bigint)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLocationRequest"
            example:
              location: "Warehouse B, Zone 2"
      responses:
        "200":
          description: Receipt location updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Receipt location updated successfully"
                  data:
                    $ref: "#/components/schemas/WarehouseReceipt"
        "400":
          description: Missing or invalid location field
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/{receiptId}/reject:
    put:
      tags:
        - Receipts
      summary: Reject warehouse receipt
      description: Mark a warehouse receipt as rejected with a specified reason
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: receiptId
          schema:
            type: string
          required: true
          description: Unique receipt identifier (bigint)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectReceiptRequest"
      responses:
        "200":
          description: Receipt rejected successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse Receipt rejected successfully"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/{receiptId}/summary:
    get:
      tags:
        - Receipts
      summary: Get receipt summary (status, totals, counts)
      description: Retrieve quick summary with total pieces, weight, and freight count
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: receiptId
          schema:
            type: string
          required: true
          description: Unique receipt identifier (bigint)
      responses:
        "200":
          description: Receipt summary retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: "#/components/schemas/ReceiptSummary"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/warehouse-receipt/{receiptId}/audit-logs:
    get:
      tags:
        - Receipts
      summary: Get audit logs for a warehouse receipt
      description: Retrieve all audit logs associated with a specific warehouse receipt, showing status changes and updates over time.
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: receiptId
          schema:
            type: string
          required: true
          description: Unique receipt identifier (bigint)
      responses:
        "200":
          description: Audit logs retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/AuditLogEntry"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/verification/{verificationId}:
    get:
      tags:
        - Receipts
      summary: Get receipts by verification ID
      description: Retrieve all receipts associated with an ID verification form
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: verificationId
          schema:
            type: string
          required: true
          description: Verification ID (bigint)
      responses:
        "200":
          description: Receipts retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/WarehouseReceipt"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/customer-station:
    get:
      tags:
        - Receipts
      summary: Get receipts by customer and station
      description: Retrieve all receipts for a specific customer/station combination
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: customerId
          schema:
            type: integer
          required: true
          description: Customer ID
        - in: query
          name: stationId
          schema:
            type: integer
          required: true
          description: Station ID
      responses:
        "200":
          description: Receipts retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/WarehouseReceipt"
        "400":
          description: customerId and stationId are required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/pro-detail/{pro}:
    get:
      tags:
        - Receipts
      summary: Get PRO header details by PRO number
      description: |
        Retrieve detailed PRO (Progressive Rotating Order) information for a given PRO number.

        **Flow:**
        1. Checks database first (Warehouse_RM_Pro_Detail table)
        2. If not found, reads from CSV file in FTP folder
        3. Validates and saves CSV data to database for future use
        4. Checks for duplicate warehouse receipts (prevents reuse)
        5. Returns formatted response with all PRO details

        **Response Fields:**
        - proDetailId: Unique identifier for the PRO detail record
        - driver: Driver number handling the shipment
        - shipper: Name of the shipper
        - shipperAccountNumber: Account number of the shipper
        - consignee: Customer/consignee name
        - consigneeAccountNumber: Customer account number
        - customerReferenceNumber: Reference number provided by customer
        - carrier: Carrier company name
        - city: Destination city
        - weight: Total weight in the shipment
        - pieces: Number of pieces in shipment
        - proNumber: PRO (Progressive Rotating Order) number
        - proDate: Date the PRO was created
        - hazmat: Hazardous materials indicator (Y/N)
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: pro
          schema:
            type: string
          required: true
          description: PRO (Progressive Rotating Order) number
          example: "21987292"
      responses:
        "200":
          description: PRO header details retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      proDetailId:
                        type: integer
                        example: 1
                      driver:
                        type: string
                        example: "D123"
                      shipper:
                        type: string
                        example: "Test Shipper Inc"
                      shipperAccountNumber:
                        type: string
                        example: "ACC123"
                      consignee:
                        type: string
                        example: "ABC Customer"
                      consigneeAccountNumber:
                        type: string
                        example: "CACC456"
                      customerReferenceNumber:
                        type: string
                        description: Optional customer reference
                        example: "REF789"
                      carrier:
                        type: string
                        example: "Shiva Test Carrier"
                      city:
                        type: string
                        example: "New York"
                      weight:
                        type: integer
                        example: 100
                      pieces:
                        type: integer
                        example: 5
                      proNumber:
                        type: string
                        example: "PRO123456"
                      proDate:
                        type: string
                        format: date
                        example: "2026-05-08"
                      hazmat:
                        type: string
                        enum: ["Y", "N"]
                        example: "N"
        "400":
          description: PRO number is required
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "PRO number is required"
        "404":
          description: PRO detail not found in database or CSV file
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: "No PRO detail found for PRO number: PRO123456 (not in database or CSV file)"
        "409":
          description: Duplicate - Carrier and PRO already added in another Record
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  message:
                    type: string
                    example: 'Duplicate - Carrier "Shiva Test Carrier" with PRO "PRO123456" already added in another Record'
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/warehouse-receipt/label-print:
    post:
      tags:
        - Receipts
      summary: Generate and print labels for a warehouse receipt
      description: >
        Generate label data for a receipt and send to printer.  
        - **printerIP**, **printerPort**, and **receiptNumber** must be provided as query parameters.  
        - Additional label data can be provided in the request body.  
        - If full label payload is given in the body, it will be used directly.  
        - If not, the service will fetch missing data from the database using the receiptNumber.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: printerIP
          required: true
          schema:
            type: string
          description: IP address of the target printer
        - in: query
          name: printerPort
          required: true
          schema:
            type: integer
          description: Port number of the target printer
        - in: query
          name: receiptNumber
          required: true
          schema:
            type: integer
          description: Warehouse receipt number to print
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LabelPrintRequest"
            examples:
              fullPayload:
                summary: Full label payload provided
                value:
                  customerName: "ACME Corp"
                  packageId: "PKG123"
                  shipper: "John Doe"
                  carrierName: "FedEx"
                  proNumber: "PRO987"
                  destination: "New York"
                  pieces: 10
                  labelCount: 2
              minimalPayload:
                summary: Only receiptNumber in query, no body
                value: {}
      responses:
        "200":
          description: Labels generated and sent to printer successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Label print sent successfully"
                  data:
                    type: object
                    properties:
                      printedTo:
                        type: string
                        example: "192.168.1.100"
                      printerPort:
                        type: integer
                        example: 9100
                      printData:
                        $ref: "#/components/schemas/LabelPrintData"
        "400":
          description: Invalid request - missing printerIP, printerPort, or receiptNumber
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Receipt not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/rate-ready-for-approval:
    post:
      tags:
        - Receipts
      summary: Mark a warehouse receipt rate as ready for approval
      description: "Update the receipt rate details and mark the receipt as ready for approval. The receipt ID is passed as a query parameter and the rate payload is sent in the request body."
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: receiptId
          required: true
          schema:
            type: integer
          description: Warehouse receipt ID to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WarehouseReceiptRateReadyForApprovalRequest"
            example:
              rateDetails:
                rate: 125.5
                dimFactor: 1.25
                baseRate: 100
                minRate: 90
                maxRate: 150
                hasFlatRate: "N"
                notesForFlatRate: null
      responses:
        "200":
          description: Warehouse receipt rate marked ready for approval successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse Receipt statuses updated successfully"
        "400":
          description: Invalid request - missing receiptId or rateDetails
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/rate-approve:
    put:
      tags:
        - Receipts
      summary: Approve warehouse receipt rate for one or more receipts
      description: "Approve rate updates for one or more warehouse receipts. The request body must include an array of receipt IDs."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToggleWarehouseReceiptStatusRequest"
            example:
              receiptIds: [100000001, 100000002]
      responses:
        "200":
          description: Warehouse receipt statuses updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse Receipt statuses updated successfully"
        "400":
          description: Invalid request - missing receiptIds
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/account-hold:
    put:
      tags:
        - Receipts
      summary: Set account hold for one or more warehouse receipts
      description: "Place one or more warehouse receipts on account hold. The request body must include an array of receipt IDs."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToggleWarehouseReceiptStatusRequest"
            example:
              receiptIds: [100000001, 100000002]
      responses:
        "200":
          description: Warehouse receipt statuses updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse Receipt statuses updated successfully"
        "400":
          description: Invalid request - missing receiptIds
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/warehouse-receipt/account-hold-revert:
    put:
      tags:
        - Receipts
      summary: Revert account hold for one or more warehouse receipts
      description: "Remove account hold from one or more warehouse receipts. The request body must include an array of receipt IDs."
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevertWarehouseReceiptStatusRequest"
            example:
              receiptIds: [100000001, 100000002]
      responses:
        "200":
          description: Warehouse receipt statuses updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Warehouse Receipt statuses updated successfully"
        "400":
          description: Invalid request - missing receiptIds
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    CreateTemporaryWarehouseReceipt:
      type: object
      required:
        - verificationId
        - customerId
        - stationId
        - carrierId
        - status
        - shipper
        - receivedBy
        - location
        - destination
        - proNumber
        - packageId
      properties:
        verificationId:
          type: integer
          example: 100000033
          description: ID verification record ID
        shipper:
          type: string
          example: "ABC Logistics Inc"
          description: Shipper name
        customerId:
          type: integer
          example: 51
        stationId:
          type: integer
          example: 93
        carrierId:
          type: integer
          example: 52
        status:
          type: string
          enum: [INITIATE, IN_PROGRESS, COMPLETED, HOLD]
          example: "INITIATE"
          description: Initial receipt status
        receivedBy:
          type: string
          example: "John Warehouse"
          description: Person who received the shipment
        location:
          type: string
          example: "Warehouse A, Zone 1"
          description: Storage location
        destination:
          type: string
          example: "Final Destination"
          description: Destination address
        proNumber:
          type: string
          example: "PRO1234511"
          description: PRO number
        packageId:
          type: string
          example: "PKG-001"
          description: Package ID

    WarehouseReceiptTemp:
      type: object
      properties:
        receiptNumber:
          type: integer
          example: 1001
          description: Auto-generated receipt number
        verificationId:
          type: integer
          example: 100000021
        receiptDate:
          type: string
          format: date-time
          example: "2026-04-06T10:00:00Z"
          description: Auto-generated timestamp
        shipper:
          type: string
          example: "ABC Logistics Inc"
        customerId:
          type: integer
          example: 51
        stationId:
          type: integer
          example: 93
        carrierId:
          type: integer
          example: 52
        receivedBy:
          type: string
          example: "John Warehouse"
        location:
          type: string
          example: "Warehouse A, Zone 1"
        destination:
          type: string
          example: "Final Destination"
        proNumber:
          type: string
          example: "PRO-2026-001"
        packageId:
          type: string
          example: "PKG-001"
        createdAt:
          type: string
          format: date-time
          description: Timestamp when record was created
        createdBy:
          type: integer
          example: 1
          description: User ID from authenticated request
        status:
          type: string
          example: "INITIATE"

    FreightImage:
      type: object
      properties:
        imageId:
          type: integer
          example: 456
          description: Unique image identifier
        freightId:
          type: integer
          example: 101
          description: Reference to freight item
        imagePath:
          type: string
          example: "uploads/freight-images/image-1649000000000.jpg"
          description: Server path to stored image
        uploadedAt:
          type: string
          format: date-time
          example: "2026-04-07T10:30:00Z"
          description: Timestamp when image was uploaded

    FreightInfo:
      type: object
      properties:
        freightId:
          type: integer
          example: 101
        receiptId:
          type: integer
          example: 1
        pieces:
          type: integer
          example: 20
        type:
          type: string
          example: "Box"
        weight:
          type: number
          example: 2000
        length:
          type: number
          nullable: true
          example: 10
        width:
          type: number
          nullable: true
          example: 10
        height:
          type: number
          nullable: true
          example: 10
        imageId:
          type: integer
          nullable: true
        images:
          type: array
          items:
            $ref: "#/components/schemas/FreightImage"
          description: "Array of images associated with this freight item"

    CreateFreightDetail:
      type: object
      required:
        - pieces
        - type
      properties:
        pieces:
          type: integer
          example: 20
          description: Number of pieces for this freight item
        type:
          type: string
          example: "Box"
          description: Type of freight (Box, Pallet, Skid, etc.)
        weight:
          type: number
          nullable: true
          example: 2000
          description: Total weight in kg
        length:
          type: number
          nullable: true
          example: 10
          description: Length in inches
        width:
          type: number
          nullable: true
          example: 10
          description: Width in inches
        height:
          type: number
          nullable: true
          example: 10
          description: Height in inches
        cubicMeter:
          type: number
          nullable: true
          example: 20
          description: Cubic meter volume
        imageId:
          type: integer
          nullable: true
          description: Reference to image ID if available
        images:
          type: array
          items:
            type: string
          example: []
          nullable: true
          description: "Optional: Array of image file paths (for JSON response) or array of image files (when using multipart/form-data upload)"

    CreateWarehouseReceiptWithFreight:
      type: object
      required:
        - receipt
        - freightDetails
      properties:
        receipt:
          type: object
          required:
            - customerId
            - stationId
            - verificationId
            - carrierId
            - status
            - entityId
            - noteThreadId
          properties:
            receiptDate:
              type: string
              format: date-time
              example: "2026-04-06T10:00:00Z"
              description: Auto-generated if not provided
            shipper:
              type: string
              nullable: true
              example: "ABC Logistics Inc"
            customerId:
              type: integer
              example: 51
            stationId:
              type: integer
              example: 93
            verificationId:
              type: integer
              example: 100000021
            carrierId:
              type: integer
              example: 52
            destination:
              type: string
              nullable: true
              example: "Customer Final Destination"
            originalDgd:
              type: string
              nullable: true
              example: "Y"
            unNumber:
              type: array
              items:
                type: string
              nullable: true
              example: ["123", "333", "3333"]
              description: "List of UN numbers"
            class:
              type: array
              items:
                type: string
              nullable: true
              example: ["2", "3"]
              description: "Hazard class values"
            properShippingName:
              type: string
              nullable: true
              example: "OXYGEN, COMPRESSED"
            hazardousDescription:
              type: string
              nullable: true
              example: "Contains extremely hazardous material"
            notes:
              type: string
              nullable: true
              example: "Handle with care"
            proNumber:
              type: string
              nullable: true
              example: "PRO-2026-001"
            piecesInland:
              type: integer
              nullable: true
              example: 50
            weightInland:
              type: number
              nullable: true
              example: 5000
            documents:
              type: string
              enum: ["Y", "N"]
              nullable: true
              example: "Y"
              description: "Documents present (Y/N)"
            receiptType:
              type: string
              nullable: true
              example: "Regular"
              description: "Receipt type (e.g., Regular, Special, etc.)"
            status:
              type: string
              enum:
                [
                  INITIATE,
                  ON-HAND,
                  REJECTED,
                  PREPARED,
                  SCANNED,
                  SHIPPED,
                  ARCHIVED,
                ]
              example: "INITIATE"
            entityId:
              type: integer
              example: 1
              description: Entity ID created for this receipt
            noteThreadId:
              type: integer
              example: 1
              description: Note thread ID created for this receipt
        freightDetails:
          type: array
          items:
            $ref: "#/components/schemas/CreateFreightDetail"
          example:
            - pieces: 20
              type: "Box"
              weight: 2000
              length: 10
              width: 10
              height: 10
            - pieces: 30
              type: "Pallet"
              weight: 3000
              length: 48
              width: 40
              height: 60

    WarehouseReceiptWithFreightResponse:
      allOf:
        - $ref: "#/components/schemas/WarehouseReceipt"
        - type: object
          properties:
            freightInfos:
              type: array
              items:
                $ref: "#/components/schemas/FreightInfo"
              example:
                - freightId: 101
                  receiptId: 1
                  pieces: 20
                  type: "Box"
                  weight: 2000
                  length: 10
                  width: 10
                  height: 10
                  imageId: null
                - freightId: 102
                  receiptId: 1
                  pieces: 30
                  type: "Pallet"
                  weight: 3000
                  length: 48
                  width: 40
                  height: 60
                  imageId: null
    WarehouseReceipt:
      type: object
      properties:
        receiptId:
          type: string
          example: "100000001"
        receiptNumber:
          type: string
          example: "100000001"
        receiptDate:
          type: string
          format: date
          example: "2026-03-31"
        receivedBy:
          type: string
          example: "John Warehouse"
        location:
          type: string
          example: "Warehouse A, Zone 1"
        shipper:
          type: string
          nullable: true
          example: "ABC Logistics"
        customerId:
          type: integer
          example: 5
        stationId:
          type: integer
          example: 10
        verificationId:
          type: string
          example: "100000001"
        carrierId:
          type: integer
          example: 1
        proNumber:
          type: string
          nullable: true
          example: "PRO-2026-001"
        piecesInland:
          type: integer
          nullable: true
          example: 50
        weightInland:
          type: integer
          nullable: true
          example: 5000
        documents:
          type: string
          enum: ["Y", "N"]
          nullable: true
          example: "Y"
        receiptType:
          type: string
          nullable: true
          example: "Regular"
        status:
          type: string
          enum:
            [INITIATE, ON-HAND, REJECTED, PREPARED, SCANNED, SHIPPED, ARCHIVED]
          example: "INITIATE"
        createdAt:
          type: string
          format: date-time
        createdBy:
          type: integer
        updatedAt:
          type: string
          format: date-time
          nullable: true
        updatedBy:
          type: integer
          nullable: true

    WarehouseReceiptWithDetails:
      allOf:
        - $ref: "#/components/schemas/WarehouseReceipt"

    ReceiptSummary:
      type: object
      properties:
        receiptNumber:
          type: string
          example: "100000001"
        proNumber:
          type: string
          nullable: true
        status:
          type: string
          example: "INITIATE"
        customerId:
          type: integer
        stationId:
          type: integer
        carrierId:
          type: integer
        totalPieces:
          type: integer
          example: 50
        totalWeight:
          type: integer
          example: 5000
        freightCount:
          type: integer
          example: 5
        createdAt:
          type: string
          format: date-time
        createdBy:
          type: integer

    FreightUpdateItem:
      type: object
      properties:
        freightId:
          type: integer
          nullable: true
        pieces:
          type: integer
          example: 10
        type:
          type: string
          example: "Pallet"
        length:
          type: number
          nullable: true
          example: 40
        width:
          type: number
          nullable: true
          example: 48
        height:
          type: number
          nullable: true
          example: 45
        weight:
          type: number
          nullable: true
          example: 200
        cubicMeter:
          type: number
          nullable: true
          example: 2.8
        images:
          type: array
          items:
            type: string
          description: Existing image paths to keep or add when using JSON-only updates.
        removeImagePaths:
          type: array
          items:
            type: string
          description: Image path values to remove from this freight item.
    UpdateReceiptRequest:
      type: object
      properties:
        location:
          type: string
          example: "Warehouse B, Zone 2"
        receivedBy:
          type: string
          example: "Jane Doe"
        shipper:
          type: string
          example: "XYZ Logistics"
        customerId:
          type: integer
          example: 51
        stationId:
          type: integer
          example: 93
        verificationId:
          type: integer
          example: 100000021
        carrierId:
          type: integer
          example: 52
        piecesInland:
          type: integer
          example: 50
        weightInland:
          type: integer
          example: 5000
        reWeight:
          type: integer
          nullable: true
          example: 5050
        proNumber:
          type: string
          example: "PRO-2026-001"
        invoiceNumber:
          type: string
          nullable: true
        poNumber:
          type: string
          nullable: true
        customerRefNumber:
          type: string
          nullable: true
        freightCondition:
          type: string
          example: "Y"
        documents:
          type: string
          enum: ["Y", "N"]
          nullable: true
          example: "Y"
        handlingDescription:
          type: string
          nullable: true
        destination:
          type: string
          example: "Customer Final Destination"
        originalDgd:
          type: string
          nullable: true
          example: "Y"
        unNumber:
          type: array
          items:
            type: string
        class:
          type: array
          items:
            type: string
        packageId:
          type: string
          nullable: true
        properShippingName:
          type: string
          nullable: true
        hazardousDescription:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        status:
          type: string
          enum:
            [
              "INITIATED",
              "ON_HAND",
              "PREPARED",
              "SCANNED",
              "SHIPPED",
              "REJECTED",
              "ARCHIVED",
            ]
          example: "ON_HAND"
        receiptType:
          type: string
          nullable: true
          example: "Regular"
        bandedSkid:
          type: string
          enum: ["Y", "N"]
          example: "Y"
        shrinkWrappedSkid:
          type: string
          enum: ["Y", "N"]
          example: "Y"
        shtIppcSkid:
          type: string
          enum: ["Y", "N"]
          example: "Y"
        plasticSkid:
          type: string
          enum: ["Y", "N"]
          example: "Y"
        hazMat:
          type: string
          enum: ["Y", "N"]
          example: "Y"
        labelCount:
          type: integer
          example: 5
        toEmails:
          type: array
          items:
            type: string
          description: Array of email addresses to send notifications to.
        cubicMeter:
          type: number
          nullable: true
          example: 2.8

        freightDetails:
          type: array
          items:
            $ref: "#/components/schemas/FreightUpdateItem"
          description: Array of freight items to add or update on the receipt.
        removeFreightIds:
          type: array
          items:
            type: integer
          description: Freight IDs to delete from this receipt.
        badFreightImages:
          type: array
          items:
            type: string
          description: Bad freight image paths to add to the receipt.
        removeBadFreightImagePaths:
          type: array
          items:
            type: string
          description: Bad freight image paths to remove from the receipt.

    UpdateLocationRequest:
      type: object
      required:
        - location
      properties:
        location:
          type: string
          example: "Warehouse B, Zone 2"
          description: Storage location for the warehouse receipt

    ToggleWarehouseReceiptStatusRequest:
      type: object
      required:
        - receiptIds
      properties:
        receiptIds:
          type: array
          minItems: 1
          items:
            type: integer
          description: Array of warehouse receipt IDs to update
          example: [100000001, 100000002]

    RevertWarehouseReceiptStatusRequest:
      type: object
      required:
        - receiptIds
      properties:
        receiptIds:
          type: array
          minItems: 1
          items:
            type: integer
          description: Array of warehouse receipt IDs to remove from account hold
          example: [100000001, 100000002]

    WarehouseReceiptRateReadyForApprovalRequest:
      type: object
      required:
        - rateDetails
      properties:
        rateDetails:
          type: object
          required:
            - rate
            - dimFactor
            - baseRate
            - minRate
            - maxRate
            - hasFlatRate
          properties:
            rate:
              type: number
              example: 125.5
            dimFactor:
              type: number
              example: 1.25
            baseRate:
              type: number
              example: 100
            minRate:
              type: number
              example: 90
            maxRate:
              type: number
              example: 150
            hasFlatRate:
              type: string
              enum: ["Y", "N"]
              example: "N"
            notesForFlatRate:
              type: string
              nullable: true
              example: null

    PaginationInfo:
      type: object
      properties:
        page:
          type: integer
          example: 1
        pageSize:
          type: integer
          example: 10

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "Error message details"

    BatchProcessWarehouseReceipts:
      type: object
      required:
        - receipts
      properties:
        receipts:
          type: array
          minItems: 1
          items:
            type: object
            required:
              - receipt
              - freightDetails
            properties:
              receipt:
                type: object
                description: "Receipt data: if receiptId present = UPDATE, if absent = CREATE"
                properties:
                  receiptId:
                    type: integer
                    example: 10
                    description: "Optional: If present, receipt will be updated; if absent, new receipt will be created"
                  receiptNumber:
                    type: integer
                    example: 100000015
                    description: "Receipt number for reference"
                  receivedBy:
                    type: string
                    nullable: true
                    example: "John Warehouse"
                  location:
                    type: string
                    nullable: true
                    example: "Warehouse A, Zone 1"
                  shipper:
                    type: string
                    example: "ABC Logistics Inc"
                  customerId:
                    type: integer
                    example: 51
                  stationId:
                    type: integer
                    example: 93
                  verificationId:
                    type: integer
                    example: 100000021
                  carrierId:
                    type: integer
                    example: 52
                  piecesInland:
                    type: integer
                    nullable: true
                    example: 50
                  weightInland:
                    type: number
                    nullable: true
                    example: 5000
                  reWeight:
                    type: number
                    nullable: true
                    example: 5050
                  proNumber:
                    type: string
                    nullable: true
                    example: "PRO-2026-001"
                  invoiceNumber:
                    type: string
                    nullable: true
                  poNumber:
                    type: string
                    nullable: true
                  customerRefNumber:
                    type: string
                    nullable: true
                  freightCondition:
                    type: string
                    nullable: true
                  documents:
                    type: string
                    enum: ["Y", "N"]
                    nullable: true
                    example: "Y"
                    description: "Documents present (Y/N)"
                  handlingDescription:
                    type: string
                    nullable: true
                  destination:
                    type: string
                    nullable: true
                  originalDgd:
                    type: string
                    nullable: true
                  unNumber:
                    type: array
                    items:
                      type: string
                    nullable: true
                    example: ["123", "333", "3333"]
                    description: "List of UN numbers"
                  class:
                    type: array
                    items:
                      type: string
                    nullable: true
                    example: ["2", "3"]
                    description: "Hazard class values"
                  packageId:
                    type: string
                    nullable: true
                  properShippingName:
                    type: string
                    nullable: true
                  hazardousDescription:
                    type: string
                    nullable: true
                  notes:
                    type: string
                    nullable: true
                    example: "Handle with care"
                  status:
                    type: string
                    enum: [INITIATE, ON-HAND, REJECTED, HOLD]
                    example: "INITIATE"
                  receiptType:
                    type: string
                    nullable: true
                    example: "Regular"
                    description: "Receipt type (e.g., Regular, Special, etc.)"
                  withSkid:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  bandedSkid:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  shrinkWrappedSkid:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  shtIppcSkid:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  plasticSkid:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  hazMat:
                    type: string
                    enum: ["Y", "N"]
                    example: "Y"
                  labelCount:
                    type: integer
                    nullable: true
                    example: 0
                  driverName:
                    type: string
                    nullable: true
                    example: "John Doe"
                  cubicMeter:
                    type: number
                    nullable: true
                    example: 100.5
              freightDetails:
                type: array
                minItems: 1
                items:
                  $ref: "#/components/schemas/CreateFreightDetail"
                description: "Array of freight items. For UPDATE, existing items are deleted and replaced with these"
          description: "Array of receipt objects to process. Each has receipt (with receiptNumber and optional receiptId) and freightDetails. Presence of receiptId determines UPDATE vs CREATE. receiptNumber is required for reference/audit in both operations."

    BatchReceiptUpdate:
      description: "DEPRECATED - See BatchProcessWarehouseReceipts for current structure"
      type: object

    BatchReceiptCreate:
      description: "DEPRECATED - See BatchProcessWarehouseReceipts for current structure"
      type: object

    BatchProcessResponse:
      type: object
      properties:
        updated:
          type: array
          items:
            $ref: "#/components/schemas/WarehouseReceiptWithFreightResponse"
          description: Array of receipts that were updated - includes all receipt data and freightInfos array with images
        created:
          type: array
          items:
            $ref: "#/components/schemas/WarehouseReceiptWithFreightResponse"
          description: Array of receipts that were created - includes all receipt data and freightInfos array with images
        totalUpdated:
          type: integer
          example: 1
          description: Total number of receipts updated in batch
        totalCreated:
          type: integer
          example: 1
          description: Total number of receipts created in batch
        filesProcessed:
          type: integer
          example: 5
          nullable: true
          description: Number of image files processed (only included when multipart/form-data with images is used)
    RejectReceiptRequest:
      type: object
      required:
        - rejectionReason
      properties:
        rejectionReason:
          type: string
          example: "Damaged goods"
          description: Reason for rejecting the warehouse receipt
